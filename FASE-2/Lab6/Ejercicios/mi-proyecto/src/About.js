// src/About.js
import React from "react";

const About = () => (
  <section>
    <h1>Acerca de BookVerse</h1>
    <p>
      Proyecto académico que integra: instalación (CRA), reglas JSX, componentes y props,
      manejo de estado y formularios, ciclo de vida con fetch y enrutamiento básico.
    </p>
  </section>
);

export default About;
